version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: RszeUfGKcjQsagwrjeySnmQubtuVRefj
      MYSQL_DATABASE: railway